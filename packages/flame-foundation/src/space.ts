/**
 * The space key controls all the following properties
 *
 * margin, margin-top, margin-right, margin-bottom, margin-left,
 * margin-block, margin-block-end, margin-block-start, margin-inline,
 * margin-inline-end, margin-inline-start, padding, padding-top, padding-right,
 * padding-bottom, padding-left, padding-block, padding-block-end,
 * padding-block-start, padding-inline, padding-inline-end, padding-inline-start,
 * inset, inset-block, inset-block-end, inset-block-start, inset-inline,
 * inset-inline-end, inset-inline-start, top, right, bottom, left, grid-gap,
 *  grid-column-gap, grid-row-gap, gap, column-gap, row-gap
 */

// Initial spacing scale
const space: any = [
  0,
  '0.375rem',
  '0.75rem',
  '1.125rem',
  '1.5rem',
  '1.875rem',
  '2.25rem',
  '2.625rem',
  '3rem',
  '3.375rem',
  '3.75rem',
];

// Spacing aliases
space.tight = '';

export { space };
