import * as React from 'react';
import { ModalActions } from '@lightspeed/design-system-react';

/**
 * Footer of the Modal
 */
export const ModalFooter: React.FC = props => <ModalActions {...props} />;
