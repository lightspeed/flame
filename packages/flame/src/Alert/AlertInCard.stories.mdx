import { Story, Preview, Meta, Props } from '@storybook/addon-docs/blocks';
import { AlertInCard } from './.';
import { Card, CardSection } from '../Card';
import { DocsContainer } from '../../.storybook/preview';

<Meta
  title="Components/AlertInCard"
  component={AlertInCard}
  argTypes={{
    onClose: {
      type: null,
    },
    type: {
      control: {
        type: 'select',
        options: ['info', 'warning', 'success', 'danger'],
      },
    },
  }}
/>

# AlertInCard

A specially stylized Alert meant to be used within the `<Card />` component.

## Types

<Story name="types">
  <div>
    <h3>Info (default)</h3>
    <AlertInCard noCloseBtn>This is an AlertInCard component</AlertInCard>
    <h3>Danger</h3>
    <AlertInCard type="danger" noCloseBtn>
      This is an AlertInCard component
    </AlertInCard>
    <h3>Success</h3>
    <AlertInCard type="success" noCloseBtn>
      This is an AlertInCard component
    </AlertInCard>
    <h3>Warning</h3>
    <AlertInCard type="warning" noCloseBtn>
      This is an AlertInCard component
    </AlertInCard>
  </div>
</Story>

## Playground

<Preview>
  <Story name="basic">
    {props => (
      <Card>
        <CardSection>
          <AlertInCard {...props}>
            This is an AlertInCard component of type "{props.type}"
          </AlertInCard>
        </CardSection>
      </Card>
    )}
  </Story>
</Preview>

## Props

<Props story="basic" />
