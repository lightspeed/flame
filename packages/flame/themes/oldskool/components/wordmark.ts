import { colors } from '../colors';

const wordmarkStyles: WordmarkStyles = {
  border: colors.maple,
  light: {
    color: colors.snow,
  },
  dark: {
    color: colors.night,
  },
};

export { wordmarkStyles };
