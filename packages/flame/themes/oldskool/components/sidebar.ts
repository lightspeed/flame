import { colors } from '../colors';

const sidebarStyles: SidebarStyles = {
  background: colors.dive,
  borderColor: colors['seal-200'],
  iconDetails: colors.dive,
  header: {
    color: colors.gray,
  },
  menulink: {
    normal: {
      color: colors.blue,
    },
    danger: {
      color: colors['maple-200'],
    },
  },
  lockButton: {
    borderColor: colors['seal-200'],
  },
};

export { sidebarStyles };
