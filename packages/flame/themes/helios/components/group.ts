import { colors } from '../colors';

// Helios 2 Group Styles
const groupStyles: GroupStyles = {
  addon: {
    background: (colors as Record<string, string>)['helios-neutral-backdrop'],
    borderColor: (colors as Record<string, string>)['helios-neutral-edge'],
  },
};

export { groupStyles };
