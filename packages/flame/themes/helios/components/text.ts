import { colors } from '../colors';

// Helios 2 Text Styles
const textStyles: TextStyles = {
  color: colors.textBody,
  link: {
    color: (colors as Record<string, string>)['helios-go-default'],
  },
};

export { textStyles };
