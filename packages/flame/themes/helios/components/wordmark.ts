import { colors } from '../colors';

const wordmarkStyles: WordmarkStyles = {
  border: colors['maple-400'],
  light: {
    color: colors.white,
  },
  dark: {
    color: colors['gray-1000'],
  },
};

export { wordmarkStyles };
