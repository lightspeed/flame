// Helios 2.0 Bone (Skeleton) Styles - Exact match with unified-components
// Using tokens from @lightspeed/unified-tokens/helios via colors object
// Skeleton components use 10% opacity of bg-neutral-inverted-top
const boneStyles: BoneStyles = {
  // Note: bg-neutral-inverted-top is #242427, 10% opacity = #2424271A
  // This is a derived value, keeping as-is for now since unified-tokens doesn't export this specific variant
  background: '#2424271A', // rgba(36, 36, 39, 0.1) - 10% opacity of bg-neutral-inverted-top
};

export { boneStyles };
